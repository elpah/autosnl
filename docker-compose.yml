services:
  base:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name:  base
    networks:
      - autosnl-network
    stdin_open: true
    tty: true
    volumes:
      - ./:/app

networks:
  autosnl-network:
    driver: bridge

volumes:
  mongo_data: {}
